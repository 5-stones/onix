
// https://ns.editeur.org/onix/en/50 
export enum MeasureUnit {
  cm = 'cm', // Centimeters
  gr = 'gr', // Grams
  in = 'in', // Inches (US)
  kg = 'kg', // Kilograms
  lb = 'lb', // Pounds (US)
  mm = 'mm', // Millimeters
  oz = 'oz', // Ounces (US)
  px = 'px', // Pixels
}